# Codex review — codex-3-robustness — gpt-5.5 xhigh, 2026-09-02

I reviewed only `/Users/virulana/.claude/whatsapp-mcp/whatsapp-bridge`, ran the allowed read-only checks, and made no file changes; verified commands: `go test ./...` passed, `go vet ./...` passed, `go build -o /tmp/wa-review-bin .` passed, `go test -race ./...` passed, `uv run pytest` passed with 109 passed and 10 known skipped scrubber cases, and `curl -sS -i http://127.0.0.1:8080/healthcheck` returned 200 with `schema_version=6`, `journal_mode=wal`, `connected=true`, and `authenticated=true`.
Findings ranked by severity are: CRITICAL none verified; HIGH H1 `sends.go:449-487` with `migrations/001_initial.sql:44-70` and `db.go:56` verified, a confirmed outbound send can return 200 and still lose the local `messages` row because the post-send insert references `messages.chat_jid` without first ensuring a `chats` parent, scenario: create a text draft to a new `recipient_jid` that is not already in `chats`, WhatsApp `SendMessage` succeeds, `UPDATE sends SET status='sent'` succeeds, `INSERT INTO messages` fails on the foreign key and only logs `sent-message persist failed`, wrong outcome: recipient has the message but local history does not, fix: after send and before inserting `messages`, upsert the direct chat parent under `persistCtx`, preferably in one post-send transaction, and surface a persistent archive-write failure if any post-send write fails; HIGH H2 `undecryptable.go:176-181`, `history_sync.go:187-196`, and `history_sync.go:546-562` verified, history sync cannot recover a row that was previously stored as `[undecryptable: ...]` because the full history-sync insert uses `ON CONFLICT(id) DO NOTHING` and the repair helper only updates empty `system` rows, scenario: live receive first emits `events.UndecryptableMessage` for message M, later an on-demand history sync delivers a decrypted proto for the same M, the code decodes the content but leaves the marker in place, wrong outcome: readable content stays hidden and healthcheck still counts it as undecryptable, fix: add a conflict update path for undecryptable markers when the history-sync proto decodes to non-empty content and keep the no-downgrade guard for real rows; HIGH H3 `aliases.go:40-53`, `aliases.go:62-83`, and `aliases.go:147-166` verified with a hypothesis on bad upstream data, alias writes accept any two non-empty different JIDs and the schema has no one-to-one or conflict state, scenario: stale or wrong LID-store data maps one phone JID to two unrelated LIDs or maps two phones to one LID, `resolveAliases` then merges histories or returns an incomplete one-hop set, wrong outcome: messages from two humans can appear as one person or a rotated alias can be missed, fix: validate that every automatic edge is exactly one `@lid` plus one `@s.whatsapp.net`, enforce one active PN per LID and one active LID set per PN with explicit conflict quarantine, and resolve connected components only after conflict checks; MEDIUM M1 `history_sync.go:187-196`, `server.go:286-289`, and `raw_type.go:71-99` verified, the full history-sync insert omits `raw_type`, scenario: a history-sync chunk inserts a new unsupported marker or empty system row after migration 006, `/healthcheck` groups only `raw_type IS NOT NULL`, wrong outcome: undecoded and legacy-empty counters underreport new history rows, fix: include `raw_type` in the history-sync insert using `rawTypeNullable(msgType, content)` and add `processHistorySyncEvent` to the invariant test noted in `raw_type_test.go:296-299`; MEDIUM M2 `media_download.go:116-135`, `content_decode.go:56-88`, `history_sync.go:181`, and `history_sync.go:225-226` verified, media-field extraction does not unwrap message envelopes even though content decoding does, scenario: an ephemeral or view-once image or voice note has its real `ImageMessage` or `AudioMessage` inside an envelope, content is classified as `image` or `voice` but `media_key` stays NULL, wrong outcome: download and transcript backfill later say media keys are unavailable, fix: call `unwrapEnvelope` at the start of `extractDownloadableFieldsFromProto` and use that shared path from history sync; MEDIUM M3 `history_sync.go:132-143`, `backfill_walk.go:362-365`, and `history_sync.go:630-639` verified, active history walks can stay pinned after failures because chat-upsert errors skip `continueWalk`, older-empty query errors return without releasing, and `SweepDecodeBackfill` does not release after `RequestChatHistory` fails, scenario: a transient SQLite error or invalid chat causes one active walk to remain in `walker.chats`, later `RequestOlderHistory` returns 409 `errWalkActive` until another sweep happens to reset it, wrong outcome: one chat can be blocked from further backfill work, fix: release the walk key on processing errors and request-send failures, and make `isActive` or the request path sweep stale entries before rejecting; MEDIUM M4 `history_sync.go:258-260`, `backfill_walk.go:368-393`, and `bridge.go:282-290` verified code and hypothesis on multi-chunk ordering, the walker advances on every history-sync conversation without checking whether `progress` is 100, scenario: WhatsApp delivers an on-demand response in partial chunks with `progress < 100`, the first partial chunk triggers the next request before the current response has fully arrived, wrong outcome: overlapping requests waste budget and can stop a walk based on an incomplete window, fix: track the original request or notification plus chunk order and only advance a walk after the final chunk or after a complete per-request aggregate; MEDIUM M5 `whisper.go:31-34` and `whisper.go:206-219` verified, transcription writes do not implement the stated idempotency guard and do not check rows affected, scenario: live enqueue and backfiller enqueue the same voice note, the slower job finishes last and overwrites the earlier transcript or a missing message row gets logged as `done`, wrong outcome: transcript state can regress or a failed DB write can look successful, fix: update with `WHERE id = ? AND voice_note_transcript IS NULL`, check `RowsAffected`, and log skipped versus written; MEDIUM M6 `main.go:149-156`, `main.go:240-256`, and `whisper.go:65-68` with `whisper.go:123-129` verified, shutdown can wait for an active transcription job because workers use a fresh background 10-minute context and `Close()` waits for them, scenario: SIGTERM arrives while `whisper-cli` or ffmpeg is processing a long file, root context is canceled and server shuts down but `transcriber.Close()` blocks until the job ends or times out, wrong outcome: supervisor restart is delayed and may be force-killed outside the graceful path, fix: give `Transcriber` a root context or shutdown context, cancel active jobs on close, and use a short drain window; MEDIUM M7 `sends_media.go:21-24`, `sends.go:296-301`, and `migrations/001_initial.sql:108-128` verified, abandoned media drafts are known unbounded growth because files and `draft` rows are only cleaned when insert fails, confirm finds the draft expired, or send succeeds, scenario: clients create media drafts and never confirm them, after one hour the rows stay `draft` and files remain under `MediaPath/outbound`, wrong outcome: disk and DB grow without a retention mechanism, fix: add a startup and periodic sweeper that marks expired drafts and removes their media files with a bounded batch size; MEDIUM M8 `aliases.go:121-135`, `aliases.go:161-184`, and `aliases.go:188` verified, `BackfillJIDAliases` swallows scan, insert, and repair errors and can return success after a partial alias pass, scenario: `SELECT contacts` starts but row iteration or `INSERT INTO jid_aliases` fails, caller logs `jid alias backfill: 0 new edges written` instead of a failure, wrong outcome: split histories remain hidden, fix: check `rows.Err`, return scan/write/repair errors with counters, and only suppress expected no-mapping cases; LOW L1 `server.go:313`, `server.go:324-337`, `server.go:351-356`, and `server.go:364-382` verified, `/healthcheck` ignores several DB query errors and can still return status `ok` when the bridge is connected, scenario: schema, transcription count, alias coverage, or decode counter queries fail while the socket is live, wrong outcome: monitoring sees healthy JSON with zeros or defaults, fix: collect probe errors and return `degraded` or 500 with an explicit `db_error` field; LOW L2 `bridge.go:193-307` verified with upstream events in `go.mau.fi/whatsmeow/types/events/events.go:94-106`, the bridge does not record `KeepAliveTimeout` or `KeepAliveRestored`, scenario: keepalive pings time out before upstream forces reconnect, `b.connected` stays true and healthcheck stays OK while sends may hang until request timeout, wrong outcome: short connectivity brownouts are invisible, fix: handle those events by storing a degraded reason, clearing it on restore, and optionally forcing reset after a configured threshold; LOW L3 `history_sync.go:106`, `history_sync.go:262-263`, `aliases.go:55`, `contacts_sync.go:169`, `undecryptable.go:193`, and `sends_reconcile.go:94-96` verified, logs include full JIDs, phone-like IDs, contact names, and per-conversation history lines, scenario: launchd or support log bundles are copied outside the machine during a large sync or alias repair, wrong outcome: PII and high-volume operational noise leave the local trust boundary, fix: hash or redact JIDs and names by default, keep raw IDs behind debug opt-in, and rate-limit per-conversation history logs; LOW L4 `migrations/001_initial.sql:44-46`, `bridge.go:422-452`, and `history_sync.go:187-196` hypothesis, `messages` is keyed only by WhatsApp message ID rather than the full message source, scenario: if two chats ever carry the same message ID, the second insert conflicts and either does nothing or upgrades the wrong row, wrong outcome: one chat loses a message and another chat can get an inconsistent last-message pointer, fix: verify real ID uniqueness in live data and upstream contracts, then migrate to a composite identity such as `(chat_jid, id)` or store a separate global key if collisions are possible.
Areas with no current finding after verification: StreamReplaced, ClientOutdated, TemporaryBan, ConnectFailure, CATRefreshError, and StreamError now have explicit cases in `bridge.go:252-269`; LoggedOut clears auth state and schedules relogin in `bridge.go:218-246` and device-deleted fatal shutdown is centralized in `auth.go:127-133` and `auth.go:287-293`; `undecryptable.go` itself has safe mode-token sanitizing and no-downgrade insert behavior in `undecryptable.go:78-113` and `undecryptable.go:176-181`; `idx_messages_chat_time` exists in `migrations/001_initial.sql:71`; WAL and message DB foreign keys are configured in `db.go:56` and `db.go:101-109`; migration files 002 through 006 are transaction-wrapped; the backfill walker has bounded maps and request budgets in `backfill_walk.go:46-60`, `backfill_walk.go:84-105`, and `backfill_walk.go:230-277`; the transcriber queue is bounded at `whisper.go:55`; no message bodies are logged in the inspected Go paths, only identifiers and names, and what is left is to fix the ranked issues above and add tests for full `processHistorySyncEvent`, raw_type invariants, envelope media extraction, post-send parent chat persistence, alias conflicts, failure release, and progress-less-than-100 chunk ordering.