# Review 4 — Python MCP server + CI — Claude/Opus agent, 2026-09-02 12:09 ART (pytest 107 passed, 10 skipped; only GET /healthcheck called)
CRITICAL 1 — request_history default path 400s: main.py:559 direction="older" sent as anchor="older"; bridge accepts only oldest|newest (server.go:138-140). The live walk succeeded only because the running MCP server still executes pre-commit Python (audit bodies carry only chat_jid+count). Zero tests import any @mcp.tool. Fix: map older→oldest, validate, respx test. VERIFIED. [FIXED 12:12 in main.py + test]
HIGH 2 — scrubbing only on content_text (main.py:503) and, in Go, only on messages.content_text at write time. Unscrubbed: chats.last_message_preview, sender_display, contacts push/full/verified names, group subject (search_groups passes through main.py:462-469). Latent: server.go:633 COALESCE(scrubbed_text, content_text) serves raw text for legacy NULL rows. Fix: _scrub_fields helper in list_chats/search_contacts/search_groups/list_messages. VERIFIED.
HIGH 3 — audit.log: appended with default umask (0644), unbounded, records every JID, every search query, full media paths; docstring says "Redacts nothing". SECURITY.md:33 claims daily rotation / 30-day retention — no code. Retention var dead on both sides. Fix: os.open 0600; prune on startup or drop the claim. VERIFIED.
HIGH 4 — scrub() quadratic (main.py:185 recomputes out.lower() per iteration) and synchronous in the event loop; 9.000×"system:" in a 63 KB message → 389 ms; list_messages limit 500 → ~3 min stall of the whole MCP server, remotely triggerable. Same defect in scrubber.go:51. Fix: lower once, splice by recorded offsets; O(n). VERIFIED (timed).
MEDIUM 5 — _bridge_get/_bridge_post single attempt, no retry; ConnectError → _bridge_unreachable tells the user to start the bridge manually — wrong during a launchd restart (~30 s today). Fix: AsyncHTTPTransport(retries=3) + one bounded backoff. VERIFIED.
MEDIUM 6 — max_rounds unbounded, raises the bridge global budget; count correctly clamped 200. Fix: clamp in main.py. VERIFIED. [FIXED 12:12: clamped 1..20]
MEDIUM 7 — docstring/threat-model drift: main.py:8-10 lists get_chat/send_file/send_audio_message (don't exist); SECURITY.md:115,117 lists 11 nonexistent read-only tools and an SSRF guard for a non-exposed tool; main.py:618 "No media/reactions/voice in v0.3.0" while send_reaction exists and version is 0.4.0. 14 real tools. Fix: generate inventory from @mcp.tool, gate in CI.
MEDIUM 8 — healthcheck tool merges /healthcheck (which never checks auth) with /api/status; if /api/status fails the tool raises. Fix: partial answer.
MEDIUM 9 — negative limits not clamped (main.py:408,494; search_contacts none); server.go:576 no lower bound → SQLite LIMIT -5 = unlimited; only search_groups handles it. Fix: max(1,min(limit,cap)).
MEDIUM 10 — zero tests of the tools; tests.yml:105-120 bypasses uv (pip install fastmcp>=3.2.4, bare pytest); ruff never runs in CI; uv.lock gitignored; pip-audit without --locked; dependabot auto-merges patches. VERIFIED (branch-protection unverifiable).
MEDIUM 11 — docs/TROUBLESHOOTING.md:118-136 stale: uv sync issue already fixed in pyproject. VERIFIED.
LOW — scrubber.go:50-56 never advances the index (infinite loop if a future pattern is a substring of [REDACTED_INJECTION]); server.go:156 echoes raw max_rounds (0 when 10 applied); .env 0644; main.py:350,363 assert for client guard.
CLEAN — download_media path handling (safeMediaStem allowlist, fixed ext map, 0600/0700; no traversal via message_id); no error swallowed into success; search_groups PII stripping works; Go↔Python pattern parity (18 each, gated); media/ and store/ perms correct.
