# Review 3 — bridge robustness — Claude/Opus agent, 2026-09-02 12:05 ART
Checks: build, vet, go test -race all clean. Only GET /healthcheck and /api/status called.
CRITICAL C1 — handleEvent (bridge.go:193-283) has no case for StreamReplaced, ClientOutdated, TemporaryBan, ConnectFailure, CATRefreshError, StreamError. whatsmeow calls expectDisconnect() on all of them, which suppresses events.Disconnected AND autoReconnect. b.connected stays true forever; /api/status says connected; sends/history proceed; inbound lost silently. Live evidence: 4x "Client outdated (405) connect failure" in bridge.err.log. Fix: handle all six; clear connected; fatal-shutdown for Replaced/Outdated/Ban (supervisor restart); delayed loginLoop for ConnectFailure/CAT/StreamError.
CRITICAL C2 — /healthcheck hardcodes status "ok" + 200 and never consults IsConnected(). Fix: include connected/authenticated/auth_state/last_sync; 503 "degraded" when disconnected or stale. C1+C2: bridge can be permanently offline and nothing reports it.
HIGH H1 — _foreign_keys=on only on session.db (bridge.go:84,86); messages.db DSN (db.go:53-61) lacks it. Comments in history_sync.go:127-129 and undecryptable.go:147-149 claim FK enforcement — false. Orphan message rows possible, invisible to list_chats/export. Fix: add to db.go DSN; audit orphans first.
HIGH H2 — history_sync.go:142-144 `continue` on chat-upsert error drops the whole conversation chunk AND skips continueWalk; walk pinned until sweepStale (15 min, only from a sweep). Fix: retry once; on failure still release the walk key.
HIGH H3 — aliases.go recordJIDAlias validates only empty/self; no user-server guard; no DELETE anywhere; BackfillJIDAliases writes alt.User into contacts.phone whenever phone IS NULL. One bad SenderAlt fuses two humans permanently (merge = HYPOTHESIS). Fix: guard both JIDs to user servers; phone repair only when phone == parsed.User; admin endpoint to delete an edge.
HIGH H4 — migrations 002-005 run as one Exec with no transaction (006 has BEGIN). Partial failure → duplicate column on next boot → OpenDB fails → log.Fatalf → launchd KeepAlive boot-loop. applyMigrations ignores rows.Err()/scan errors (db.go:159-167). Fix: BEGIN/COMMIT in 002-005; check rows.Err().
MEDIUM M1 — senderJID := chatJID fallback stores GROUP jid as sender (history_sync.go:167-177). Fix: NULL sender when group and no participant.
MEDIUM M2 — b.client.Store.ID read unlocked (history_sync.go:93-96). Fix: b.DeviceJID(); log when empty.
MEDIUM M3 — preview loop double-decodes every message (history_sync.go:115-125) and doubles the undecoded WARN. Fix: find max index, decode once.
MEDIUM M4 — logs 0644 in 0755 dir; contacts_sync.go:169 logs real name + phone; JIDs (phones) throughout; no rotation; 4483/6785 lines one repeated media-hmac WARN. No message bodies logged. Fix: 0600, hash/user-part only, rotation.
MEDIUM M5 — aliases.go rows.Err() never checked; Scan errors continue silently → partial alias set → split history. Fix: check and return.
LOW — nil-key/empty-id messages dropped silently (history_sync.go:158-161); no DELETE anywhere → sends drafts, media_downloads, audit_log grow unbounded; WHATSAPP_AUDIT_LOG_RETENTION_DAYS validated in config.go but audit_log has zero Go references (dead knob); server.go:295-316 four `_ =` scans report 0 on failure.
CLEAN — undecryptable.go (strong: sanitized mode token, ON CONFLICT upgrade path both directions); backfill_walk bounded by construction; shutdown LIFO Disconnect→transcriber.Close→db.Close correct; idx_messages_chat_time exists; WAL set and verified; origin_guard (loopback Host + Origin allowlist), server timeouts, LimitReader sound.
