# Review 2 — bridge security — Claude/Opus agent, 2026-09-02 12:08 ART (vet/test/build clean; lsof confirms 127.0.0.1:8080 only)
CRITICAL C1 — no authentication on any of 17 routes; only origin_guard (Host+Origin). Any local process can read all messages (GET /api/messages) and SEND as Juan (POST /api/sends + confirm). SECURITY.md:23 says local-process reads are in scope, mitigated by SQLCipher — the API bypasses that. Fix: boot-minted bearer token in store/bridge.token (0600), required on all routes except /healthcheck, ConstantTimeCompare; or a unix socket in a 0700 dir. VERIFIED.
CRITICAL C2 — arbitrary file read + exfiltration: sends_media.go:128-141 filepath.Clean(req.FilePath) then os.ReadFile — no root allowlist; absolute paths pass. {"send_type":"file","file_path":"/Users/virulana/.ssh/id_rsa"} + confirm → key sent over WhatsApp. Fix: allowlist roots (WHATSAPP_SEND_FILE_ROOTS), EvalSymlinks + HasPrefix. VERIFIED.
HIGH H1 — scrubber runs only on message bodies; NOT on chats.last_message_preview (bridge.go:344), chats.name, sender_display/push name (bridge.go:376), voice transcripts (whisper.go:208), contact names (server.go:717-718). Python scrubs only content_text in list_messages. A display name / group subject carries instructions verbatim. Fix: scrub at the READ boundary over every string field in response structs; mirror in Python. VERIFIED.
HIGH H2 — scrubber is an 18-entry substring list; NBSP / spaced letters evade it; no Spanish patterns. Fix: NFKC + whitespace/zero-width normalisation, Spanish patterns; treat as telemetry not control; fix SECURITY.md:37 wording. VERIFIED (ran).
HIGH H3 — scrubber splices byte offsets from a lower-cased copy of DIFFERENT length (scrubber.go:51-55; U+212A/U+0130 change byte length) → corrupts text, can leave partial payload / invalid UTF-8. Repro given. Fix: (?i) regexp ReplaceAllString on the original. VERIFIED.
MEDIUM M1 — negative limit → SQLite LIMIT -1 = unlimited (server.go:512,576; parseIntDefault). Fix: atoiDefaultPositive. VERIFIED.
MEDIUM M2 — no body limit on sends.go:76, presence.go:28,67,111 (pattern exists elsewhere). Fix: http.MaxBytesReader. VERIFIED.
MEDIUM M3 — no DisallowUnknownFields anywhere; typo'd send fields silently dropped. VERIFIED.
MEDIUM M4 — SQLCipher key passed as argv to `security add-generic-password -w` on first mint (ps-visible). Fix: file 0600 or Keychain API. VERIFIED.
MEDIUM M5 — audit.log / bridge logs / .env are 0644; .gitignore names bridge-stdout.log (hyphen) but files are bridge.out.log (dot) → `git add .` would commit 1.6 MB of logs with phones+names. Fix: chmod 600; fix patterns. VERIFIED (git add -n stages them).
MEDIUM M6 — contacts_sync.go:169 logs real name + phone. No message bodies logged. VERIFIED.
MEDIUM M7 — WHATSAPP_AUDIT_LOG_RETENTION_DAYS read+validated, never used; SECURITY.md claims rotation. VERIFIED.
MEDIUM M8 — inbound media read fully into memory, no size cap (media_download.go:190); outbound capped. VERIFIED.
LOW — L1 LIKE '%q%' no ESCAPE (?q=% dumps contacts; parameterized, not injection); L2 draft TTL checked before a 5-min media upload; L3 media/mcp-server.log 0644 inside 0700 dir.
CLEAN — SQL injection none (all bound; Sprintf only splices inClausePlaceholders); bind address enforced loopback (config.go:120-124); DNS-rebinding/CSRF guard sound (origin_guard.go:81-97, no CORS header); media path traversal impossible (safeMediaStem allowlist + SHA tag, 0600/0700); SSRF guard in fetch_url.go exemplary (HTTPS-only, per-hop, guardedDialContext); send state machine correct (status='draft' guard + RowsAffected, TTL, single SendMessage site); key never logged (whatsmeow's own logger un-audited — HYPOTHESIS).
Top three: C1, C2, H1.
